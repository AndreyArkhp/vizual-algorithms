import React from "react";
import {Pointer} from "../components/list-page/types";

export const MAX_LENGTH_INPUT = 4;
export const MAX_LENGTH_ARR = 6;
export const MIN_LINGTH_ARR = 3;
export const MAX_NUMBER_IN_ARR = 100;

export const pointerPosition: React.CSSProperties = {
  display: Pointer.Hidden,
  top: Pointer.Top,
  left: Pointer.Start,
};
